{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://springfield-ham-radio.github.io/radio-module-catalog/catalog.schema.json",
  "title": "Springfield Radio Module Catalog",
  "description": "Official index of installable radio-module packages for the Ham Radio desktop app. Each module is one manufacturer zip; radios[] lists the configs that zip actually contains.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "modules"],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Catalog format version. The app rejects unsupported versions."
    },
    "modules": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "package",
          "manufacturer",
          "version",
          "radios",
          "supportedRadios",
          "minApiVersion",
          "downloadUrl",
          "integrity"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable module id used for on-disk install paths (e.g. baofeng)."
          },
          "package": {
            "type": "string",
            "minLength": 1,
            "description": "npm package name for developers (e.g. @springfield/radio-module-baofeng)."
          },
          "manufacturer": {
            "type": "string",
            "minLength": 1
          },
          "description": {
            "type": "string"
          },
          "version": {
            "type": "string",
            "minLength": 1,
            "description": "Module package version (semver)."
          },
          "radios": {
            "type": "array",
            "minItems": 1,
            "description": "One entry per configs/*.json file in the zip. Generate this from those files; do not list marketing aliases.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["modelId", "name", "config"],
              "properties": {
                "modelId": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Radio config id.model (e.g. baofeng-uv5r)."
                },
                "name": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Radio config id.name (e.g. Baofeng UV-5R)."
                },
                "config": {
                  "type": "string",
                  "pattern": "^configs/[^/]+\\.json$",
                  "description": "Path of the config inside the zip."
                }
              }
            }
          },
          "supportedRadios": {
            "type": "array",
            "minItems": 1,
            "description": "radios[].modelId, kept for schemaVersion 1 clients.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "minApiVersion": {
            "type": "string",
            "minLength": 1,
            "description": "Minimum @springfield/ham-radio-api version required by this module."
          },
          "downloadUrl": {
            "type": "string",
            "format": "uri",
            "description": "HTTPS URL of the JSON-only module zip on GitHub Releases."
          },
          "integrity": {
            "type": "string",
            "pattern": "^sha256:[a-fA-F0-9]{64}$",
            "description": "SHA-256 of the zip as sha256:<hex>."
          }
        }
      }
    }
  }
}
